package group04.collision;

import org.junit.After;
import org.junit.AfterClass;
import org.junit.Before;
import org.junit.BeforeClass;
import org.junit.Test;

public class ProcessorTest {
    
    public ProcessorTest() {
    }
    
    @BeforeClass
    public static void setUpClass() {
    }
    
    @AfterClass
    public static void tearDownClass() {
    }
    
    @Before
    public void setUp() {
    }
    
    @After
    public void tearDown() {
    }

    /**
     * Test of isColliding method, of class Processor.
     */
    @Test
    public void testIsColliding() {
/*        System.out.println("isColliding");
        GameData gameData = new GameData();

        World world = new World();
        //add entity map        

        Entity entity = new Entity();
        //Entityloc

        float moveX = 0.0F;
        float moveY = 0.0F;
        Processor instance = new Processor();
        boolean expResult = false;
        boolean result = instance.isColliding(world, gameData, entity, moveX, moveY);
        assertEquals(expResult, result);
        // TODO review the generated test code and remove the default call to fail.
        fail("The test case is a prototype.");*/
    }

    /**
     * Test of isEntitiesColliding method, of class Processor.
     */
    @Test
    public void testIsEntitiesColliding() {
        /*
        System.out.println("isEntitiesColliding");
        World world = null;
        GameData gameData = null;
        Entity entity1 = null;
        Entity entity2 = null;
        Processor instance = new Processor();
        boolean expResult = false;
        boolean result = instance.isEntitiesColliding(world, gameData, entity1, entity2);
        assertEquals(expResult, result);
        // TODO review the generated test code and remove the default call to fail.
        fail("The test case is a prototype.");*/
    }

    /**
     * Test of process method, of class Processor.
     */
    @Test
    public void testProcess() {/*
        System.out.println("process");
        GameData gameData = null;
        World world = null;
        Processor instance = new Processor();
        instance.process(gameData, world);
        // TODO review the generated test code and remove the default call to fail.
        fail("The test case is a prototype.");*/
    }
    
}
